# assets

issue 回复里引用的截图与录屏，按 issue 编号命名。

这是一个孤立分支，与 `main` 无共同历史，只用来托管静态文件。

素材由仓外的 shot 脚本产出：真窗口真 GPU，1280×720 / 1280×800，scale 1.0，深色主题，中文界面。
GIF 由同名 MP4 转出（800px 宽 / 15fps），供 issue 正文内嵌；MP4 是原件。
